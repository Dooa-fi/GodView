[build]
builder = "RAILPACK"
buildCommand = "npm run build --workspace=@godview/dashboard"

[deploy]
startCommand = "npm run start --workspace=@godview/dashboard"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
